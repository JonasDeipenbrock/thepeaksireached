version: 2.1

orbs:
  node: circleci/node@4.9.0

jobs:
  deploy-preview:
    docker:
      - image: cimg/node:18.10.0
    steps:
      - checkout
      - run: echo "hello world"

workflows:
  preview:
    triggers:
      - schedule:
          cron: "0 0 * * *"
          filters:
            branches:
              ignore:
                - main
    jobs:
      - deploy-preview
